// Expo Router Stack Layout
import React from 'react';
import { Stack } from 'expo-router';

// Task: Create a stack layout with two screens and modal

// 🐱 Create a Stack with two screens, Home and About
// 🐱 Navigate between them using <Link>
// 🐱 Present a modal with a disclaimer.tsx file

// 🦫 Customise the header with screenOptions
// 🦫 Use a TouchableOpacity in the <Link> component

// 🦉 Import <Stack> and <Link> from 'expo-router'
// 🦉 Customise with screenOptions and options
// 🦉 Define Modals with presentation: 'modal'

const RootLayout = () => {
  return (
    <>
      {/* 🐱 Create a new <Stack> */}
      {/* 🦉 Define Screens with <Stack.Screen name="filename"> */}
    </>
  );
};

export default RootLayout;
