// Auth Scenario: Nested Layout

// Task: Create an inside tab layout

// 🐱 Create a layout for the inside tabs
// 🐱 Define two tabs for home and profile

// 🦫 Fix the duplicate header
// 🦫 Style the StatusBar light
// 🦫 Add a logout button to the home page

// 🦉 You can use headerRight to add a button to the header

const Layout = () => {
  return (
    <>
      {/* 🐱 Create an style the Tabs layout */}
      {/* 🐱 Add an customise the two tabs */}
    </>
  );
};
export default Layout;
